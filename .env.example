# Local-only portfolio defaults. Replace as needed; never commit real credentials.
DATABASE_NAME=anchor_portfolio
DATABASE_URL=jdbc:postgresql://localhost:5432/anchor_portfolio
DATABASE_USERNAME=anchor_portfolio
DATABASE_PASSWORD=anchor_portfolio_local
SERVER_PORT=8080
